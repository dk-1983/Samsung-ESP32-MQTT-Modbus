#pragma once
static const char UPDATES_PAGE[] PROGMEM=R"HTML(<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Samsung · Updates</title>__STYLE__</head><body>__NAV__<main><h1>GitHub updates / Обновления</h1><form id="form"><label><input id="enabled" type="checkbox">Automatic installation / Автоматическая установка</label><button>Save / Сохранить</button></form><p><button id="check">Check / Проверить</button> <button id="install">Install / Установить</button></p><p>Only signed stable releases for this board. Disable automatic installation to prevent updates; checking remains available. Installation briefly restarts the controller. / Только подписанные стабильные версии для этой платы. Запрет блокирует установку, но не проверку. При установке контроллер перезапускается.</p><p><a href="https://github.com/dk-1983/Samsung-ESP32-MQTT-Modbus/releases" target="_blank" rel="noopener">Release notes / Изменения версий</a></p><p id="message" role="status"></p><p id="summary"></p><progress id="progress" max="100" value="0"></progress><details><summary>Diagnostics / Диагностика</summary><pre id="status"></pre></details></main><script>
const token='__TOKEN__',el=id=>document.getElementById(id);let init=false;
async function refresh(){let r=await fetch('/updates/status',{cache:'no-store'});if(!r.ok)throw Error(await r.text());let s=await r.json();el('summary').textContent='Installed / Установлено: '+s.installed+' · Available / Доступно: '+(s.available||'—')+' · '+s.phase+(s.error?' · '+s.error:'');el('progress').value=s.download_percent;el('progress').hidden=!s.busy;el('status').textContent=JSON.stringify(s,null,2);if(!init){el('enabled').checked=s.web_enabled;init=true;}el('install').disabled=s.busy||!s.effective_enabled||s.phase!=='available';}
async function post(path,data){try{let r=await fetch(path,{method:'POST',body:new URLSearchParams({token,...data})});if(!r.ok)throw Error(await r.text());el('message').textContent='Accepted / Принято';await refresh();}catch(e){el('message').textContent=e.message;}}
el('form').onsubmit=e=>{e.preventDefault();post('/updates/config',{enabled:el('enabled').checked?'1':'0'});};el('check').onclick=()=>post('/updates/check',{});el('install').onclick=()=>post('/updates/install',{});refresh().catch(e=>el('message').textContent=e.message);setInterval(()=>refresh().catch(()=>{}),2000);
</script></body></html>)HTML";
