// SPDX-License-Identifier: MIT
#pragma once
static const char MQTT_PAGE[] PROGMEM=R"HTML(<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Samsung · MQTT</title>__STYLE__</head><body data-page="mqtt">__NAV__<main><h1>MQTT через Wi-Fi</h1><p>Подключение к внешнему брокеру в локальной сети. MQTT работает независимо от Modbus.</p><form id="form"><label><input id="enabled" type="checkbox"> Включить MQTT</label><label><input id="discovery" type="checkbox"> Обнаружение Home Assistant</label><label>Адрес брокера<input id="host" maxlength="127" placeholder="192.168.1.10 или mqtt.local"></label><label>Порт<input id="port" type="number" min="1" max="65535" value="1883" required></label><label>Пользователь<input id="username" maxlength="64" autocomplete="off"></label><label>Новый пароль<input id="password" type="password" maxlength="128" autocomplete="new-password"><small>Оставить пустым, чтобы сохранить текущий пароль.</small></label><label><input id="clear_password" type="checkbox"> Удалить сохранённый пароль</label><label>Префикс топиков<input id="prefix" maxlength="64" required></label><button id="save" disabled>Сохранить</button></form><p id="message" role="status"></p><pre class="status" id="status"></pre><p>Локальный MQTT TCP без TLS. Команды отправляйте без retain.</p><script>
const token='__TOKEN__',el=id=>document.getElementById(id);async function refresh(initial=false){let r=await fetch('/mqtt/config',{cache:'no-store'});if(!r.ok)throw Error(await r.text());let s=await r.json();if(initial){for(let k of ['host','port','username','prefix'])el(k).value=s[k];el('enabled').checked=s.enabled;el('discovery').checked=s.discovery;el('save').disabled=false}el('status').textContent=(s.connected?'Подключён к брокеру':'Нет подключения к брокеру')+'\nПароль сохранён: '+(s.password_set?'да':'нет')+'\nПрефикс: '+s.prefix;}
el('form').onsubmit=async e=>{e.preventDefault();el('save').disabled=true;try{let body=new URLSearchParams({token,discovery:el('discovery').checked?'1':'0',enabled:el('enabled').checked?'1':'0',host:el('host').value,port:el('port').value,username:el('username').value,password:el('password').value,clear_password:el('clear_password').checked?'1':'0',prefix:el('prefix').value});let r=await fetch('/mqtt/config',{method:'POST',body});if(!r.ok)throw Error(await r.text());el('password').value='';el('clear_password').checked=false;el('message').textContent='Настройки сохранены. Подключение применяется в фоне; изменение префикса перезапускает контроллер.';await refresh()}catch(e){el('message').textContent=e.message}finally{el('save').disabled=false}};
refresh(true).catch(e=>el('message').textContent=e.message);setInterval(()=>refresh().catch(()=>{}),3000);
</script></main></body></html>)HTML";
